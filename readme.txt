# 장고는 reloading 기능이 있어서 변경사항이 서버를 재시작하지 않아도 적용됨

# 뷰는 데이터를 받아오는 것만 디자인은 템플릿에서 바꾸는것이 수정에 용이 

# 템플릿을 로드하고 컨텍스트를 채우고 httpReponse 렌더링된 템플릿의 결과로 반환하는 것이 일반적

# 장고에서는 url에 name이 있어서 url이 변경되더라도 templates url에서는 소스코드를 변경할 필요가 없음

# URLconf에 네임스페이스를 추가하여 장고가 URL에 대해 생성할 앱 보기를 알수 있도록 할 수 있음

# 장고와같은 웹 프레임워크 사용에는 개발 편의성도 있지만 코드를 통해 보안을 취할 수 있음 ex) csrf_token

# generic view(클래스 기반 뷰)를 통해 코드를 줄일 수 있음 방법 : 1.URLconf 변환 2. old하거나 필요하지 않은 뷰 삭제